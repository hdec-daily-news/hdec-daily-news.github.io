name: Daily News Update

on:
  schedule:
    # KST 오전 7:30 목표 (UTC 22:30)
    - cron: '30 22 * * *'
    # KST 오후 1:00 목표 (UTC 04:00)
    - cron: '0 4 * * *'
  workflow_dispatch: # 수동 실행도 가능

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run news collector
        run: python auto_news.py

      - name: Copy to index.html
        run: cp news_top10.html index.html

      - name: Commit and push
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add index.html news_top10.html 현대건설_뉴스_종합.csv shown_articles.json
          git diff --staged --quiet || git commit -m "Daily news update $(TZ=Asia/Seoul date +%Y-%m-%d)"
          git push
